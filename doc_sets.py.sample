from __future__ import unicode_literals

import inspect, os


here = os.path.abspath(os.path.dirname(inspect.getfile(inspect.currentframe())))

DOC_SETS = {
    'dokang': {
        'path': '%s/docs/_build/html' % here,
        'url': 'file://%s/docs/_build/html/index.html' % here,
        'title': "Dokang"
    },
}
